/**
 * App-wide status types
 */

enum Status {
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR = 'error',
  IN_PROGRESS = 'in-progress',
  PENDING = 'pending',
  VALIDATING = 'validating',
  TRANSFORMING = 'transforming'
}

export default Status
